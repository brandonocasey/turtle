usage() {
  echo
  echo "  init a new turtle.json file interactively. If one"
  echo "  one exists it will use the current options as defaults"
  echo
  echo "  Usage: "
  echo "    turtle-init [-f|--force|-y|--yes]"
  echo
  echo "  -h, --help   show help for this binary"
  echo "  -y, --yes    use all defaults and dont prompt"
  echo "  -f, --force  alias for yes"
  echo
}

main() {
  :
}
